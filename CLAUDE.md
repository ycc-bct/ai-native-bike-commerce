# CLAUDE.md

捷安特（GIANT）官網「沈浸式 AI 選車」改版提案原型。此資料夾唯一產出是 `giant.html`。

## 專案背景

- 客戶：捷安特，公司大客戶。委託方是 BCT。
- 客戶原始需求只有一句「想在官網加入沈浸式 AI，協助使用者找產品」，沒有更多描述。
- 主管先做過一版 demo（`https://ycc-bct.github.io/ai-native-bike-commerce/`），**客戶打回，理由是「不想要那麼明顯 AI 推薦的樣子」**。那版是左右分割、右側常駐「Mia 選車顧問」對話框。
- 本原型是替代方案：把 AI 藏進既有介面，而不是新增一個 AI 介面。

## 最高原則：不能看起來像 AI

驗收標準：**把介面裡的「AI」兩個字全部刪掉，功能還是說得通、還是好用。**

以下元素一律不得加入（除非我明確要求）：

- ✨ 星星／閃光圖示、紫藍漸層
- 「AI 助理」「智慧推薦」「Powered by AI」等字樣
- 右下角浮動對話泡泡、對話氣泡 UI、逐字打字動畫
- 擬人化角色名稱與頭像
- 「我可以幫你…」這類助理開場白
- 標題用被動語態或「為你找到」（要用騎乘語言，主詞留給使用者）

例外：Tweaks 面板裡的「AI 字樣對照」開關是**刻意**保留這些元素的，用於簡報時現場對比。不要移除。

## 這一版的核心論點

官網現有的篩選欄有六組，**全部是規格語言**：車款類別、避震類別、車架材質、輪徑大小、煞車類別、座管類別。沒有「路面」，沒有「強度」——要用這個篩選欄，你得先懂規格。

所以新增的三排（路面／騎乘強度／價格）是**疊加在既有六組之上**，樣式完全一致，只多一個「新增」標記（Tweaks 可關掉來看它融不融得進去）。客戶看到的是「我的篩選欄多了三排」，不是「我的篩選欄被換掉了」。**不要把它改回取代式**，那會毀掉整個論點。

## 技術與檔案

- 單一 HTML 檔，無 build、無框架、無套件。雙擊即開。約 930KB（幾乎全是內嵌影像）。
- **字體與 tokens 全部對齊官網實測值，不是設計稿也不是猜的：**
  - `--brand:#06038D`（官網 logo 實測 `rgb(6,3,141)`）、`--ink:#333`、`--ink-70:#58626E`（價格色）、`--line:#DDD`
  - 字體：**Open Sans（內文）+ Overpass（數字與價格）**，走 Google Fonts。
  - **中文刻意不指定字體**，跟官網一樣交給系統字（Mac 蘋方／Windows 微軟正黑）。這是為了「一眼認得是自己的網站」而做的取捨，代價是跨作業系統中文長相不同。**不要為了統一而加回 Noto Sans TC**，那是上一版的做法，已經換掉了。
  - 導覽列高 64px、logo 置中、漢堡在最左——照官網版型，不要改成一般的左 logo 版型。
- 緩動一律 `cubic-bezier(.16,1,.3,1)`。不要引入新色相。
- 不使用 localStorage / sessionStorage。
- 影像 27 張全部內嵌成 data URI（WebP），離線可開、對外零連線（Google Fonts 除外）。
  - 素材是 GIANT 官網公開影像：20 款產品圖、首頁檔期 banner、登山車分類頁主視覺，以及問答用的四張分類頁主視覺（公路／礫石／林道／XC／E-Bike）。
  - 放在 script 開頭的 `ASSET` 物件。markup 端寫 `data-img="key"`，由 `ASSET` 統一指派 `src`，同一張圖不會被編碼兩次——新增圖片時照這個模式走，不要直接把 base64 貼進 `src`。
  - 不再外連 `ycc-bct.github.io`。那是主管被打回那版 demo 的網址，簡報時被客戶端看到很尷尬，不要加回去。

## 資料規則（重要）

- 車款資料取自 `giant-bicycles.com/tw` 登山車分類頁，**共 20 項產品**（ATX 8,800 到 Trance X Advanced 188,000），含 3 個車架單品與 2 組同名不同年式（Talon 2027／2025、Faith 24 2027／2025）。
- **`best` / `lvl` / `susp` / `mat` / `wheel` / `brake` / `post` / `kids` 這些欄位不是自訂的**，是官網篩選欄自己用的分類值（`best-for-*`、`level-*`、`suspension-*`…）。這是這一版最重要的資產：推薦邏輯建立在官網自己的分類上，不是手工標的。
  - **驗證方法**：官網每個分面都公佈數量。比對規則是 `best` 5 項、`susp` 4 項、`mat` 2 項、`wheel` 4 項、`brake` 2 項、`post` 2 項，共 18 個數字，目前全部吻合。改動 `BIKES` 後請重跑這個比對。
  - 座管是陣列（Talon 2027 車系內同時有伸縮與可調式），輪徑也是陣列。其餘為純量。
- **不得捏造規格、重量、幾何數字。** 查不到的欄位標「官網未列」，尺寸欄位標「待接官方 Size Guide」。這些空格在提案中是加分項，不要為了畫面好看而填滿。
- 上一條包含「用係數推估」。尺寸卡的座高／龍頭／把手曾經是用身高乘一個係數算出來的，已經拿掉——留一個看起來精確的假數字，比留一格空白傷害大。`SIZES` 現在只有 `s / min / max`，不要再加回幾何欄位。
- 尺寸字母（S/M/L/XL）與身高區間是**通用示意值**，畫面上一律標「示意值」。GIANT 每個車系的 Size Guide 並不相同，接上官方資料前不要拿掉這個標記。
- 推薦邏輯是純規則式評分，**刻意不接模型**：車款類別吻合 +4、騎乘等級吻合 +3、起價在預算內 +3、需要電輔而它有 +4／沒有 −3、已設條件但未指定電輔時 E-Bike −1、車架單品 −3、青少年車但身高超出官網標示 −5、起價超出預算 30% 以上 −6。修改權重時，同步更新「這份清單怎麼來的」區塊裡列出的數字。
- E-Bike 的 −1 只在 `S.touched` 為真時生效。使用者什麼都還沒選時全部同分，列表才真的等於它宣稱的價格排序。
- **青少年車的 −5 用的是官網自己標的適用年齡／身高**（Faith 24 標 130–150cm、Faith 與 STP 26 標 10–16 歲），不是推估。這條擋掉了「推一台 24 吋童車給 175cm 成人」這種簡報事故；反向也成立，140cm 的使用者第一順位就是 Faith 24。
- 車架單品留在清單裡（官網也把它們列在同一頁，拿掉會對不上 20 這個數字），但排在後面，且**永遠不會成為「幫我決定」的結論**。
- 排序是軟性的，規格分面才做硬篩。超出預算的車仍會出現在清單末端，這是刻意的：客戶要的是「協助找產品」，不是把選項關掉。
- 排序選單裡「價格由低至高／由高至低」是官網原有的兩個選項，**「適合度」是新增的**，簡報時要講明白，不要當作本來就有。選了價格排序時不套用任何適合度權重，也不出現推薦理由。

## 已完成模組

1. 首頁：官網版型（漢堡＋主選單／置中 logo／搜尋）、官網現行檔期主視覺、最新消息、footer。**行銷主視覺不得更動。**
2. 找車入口：整句自然語言 → 直接落到已套好篩選的 PLP，**不回話、不開對話視窗**
3. 四題情境問答（全螢幕、一次一題，最後一題是身高滑桿；選項圖用 GIANT 各分類頁主視覺）
4. PLP：官網分類頁主視覺與麵包屑 + **三排新增條件疊在官網原有六組之上** + 條件 chip + 建議車架尺寸卡 + 每張卡的「為什麼是這台」
5. 「幫我決定」：一鍵單一結論，含「除非——」的對照說明。對照**只陳述兩台的差異，不判斷孰優孰劣**——價差不等於規格高低（電輔車的溢價來自馬達），曾經用價差推論「你更在意規格上限」而講反過。要能排序規格層級，得等 Phase 0。
6. 規格比較：勾選 2–3 台，底部比較條，彈窗表格（欄位即官網分面）
7. 「這份清單怎麼來的」：預設收起的說明區塊
8. 以圖找車：僅入口示意，未串接模型
9. Tweaks 面板：AI 字樣對照 / 推薦理由 / 清單說明 / **新增條件標記** / 提案註解列

## 待辦

- 尺寸卡的座高／龍頭／把手三列目前是「待接官方 Size Guide」佔位，需逐車系接 GIANT 官方 Size Guide 後才填
- 首頁「登山車系」那一排是本原型自己加的（官網現行首頁該位置放的是 Propel 公路車）。要嘛接官網真正的檔期商品，要嘛在簡報時說明
- Trance X E+ 的煞車與座管欄位官網未列，比較表會顯示「官網未列」——確認是官網資料缺漏還是抓取遺漏
- Phase 0：把 20 台車的完整規格、幾何表、Size Guide 結構化成機器可讀欄位——這是上述所有功能的地基，也是可獨立報價的工作項。目前至少有三處直接卡在這裡：尺寸卡的三列佔位、規格比較表的「示意值」欄位、「除非——」無法比較規格層級。分面分類已經有了（官網自己就有），缺的是規格數值本身。

## 工作方式

- 改完直接覆蓋 `giant.html`，不要另存 v2 / v3。需要保留舊版時我會說。
- 回報時只講重要決策與需要我判斷的地方，不要逐項複述做了什麼。

## 店員模式（ambient 分支，2026-08-21 新增）

把「AI 在背景理解瀏覽旅程，需要時才出面」做成像實體店的店員：平常讓你自己逛，逛一陣子或來回比較時才走近；你也可以隨時主動找（找車入口／問答／搜尋都會進入「一起逛」）。

- **狀態**：`J`（只活在記憶體，不寫 storage）記錄看過哪台（卡片停留 1.5 秒或點開）、改了幾次篩選、比較清單閒置多久、PLP 停留與捲動深度。`jInfer()` 只在使用者**沒設**的欄位上，用看過的車推斷路面／強度／預算，欄位值仍是官網分類。
- **走近時機 `jShould()`**：進站 30 秒內不出現；來回看同一台 ≥2 次或看了 ≥4 台、改條件 ≥3 次還沒點任何車、比較清單放著 20 秒、PLP 停留 45 秒且捲過 60%。被按「先自己逛」後 180 秒不再來、沒理它 90 秒不再來、一次造訪最多兩次。
- **走近的樣子**：`body.amb-near` → 視窗四周 2px 品牌藍外框（`.amb-frame`）＋底邊一條白色 tab（`.amb-tab`，不是對話泡泡、沒有角色）。文案由 `jCopy()` 依事實生成，主詞是使用者。12 秒沒理自動退回。
- **一起逛**：`body.amb-on` → 外框轉淡、清單以 FLIP 滑到新位置（`jFlip`）、上方「你看過的」縮圖以品牌藍細線串起（`renderJStrip`，影片版 Ride Line 的克制版）、卡片帶「為什麼是這台」。「先自己逛」隨時退出。
- **點開一台車 `openPeek()`**：沿用 modal，只列官網分類欄位，不生成規格。
- **Tweaks** 新增「走近的時機」開關（關掉＝純被動，只在主動找時出現）與「立刻走近」按鈕（簡報用）。
- 仍守「把 AI 兩個字刪掉還說得通」：整層沒有 AI 字樣、沒有角色、沒有泡泡、沒有新色相。
